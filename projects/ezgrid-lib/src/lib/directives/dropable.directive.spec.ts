import { DropableDirective } from './dropable.directive';

describe('DropableDirective', () => {
  it('should create an instance', () => {
    const directive = new DropableDirective();
    expect(directive).toBeTruthy();
  });
});
